# Harness Sandboxing

`LocalSandboxRuntime` executes allowlisted commands with timeout capture. Commands outside the allowlist fail loudly. Docker/Deno runtime expansion remains feature-gated behind future runtime implementations.
