# Harness Benchmarking

Benchmark mode uses `HarnessMode::Benchmark` and requires test-level verification before completion. Benchmark tasks are represented by the benchmark module and include visible and hidden validation commands.
