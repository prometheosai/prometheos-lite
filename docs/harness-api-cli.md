# Harness API and CLI

API endpoints include `POST /api/work-contexts/:id/harness/run` plus `trajectory`, `artifacts`, `confidence`, `replay`, `risk`, and `completion` views. CLI integration is exposed through the harness service and should route through the same WorkContext execution service path.
